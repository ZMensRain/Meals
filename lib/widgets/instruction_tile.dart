import 'package:flutter/material.dart';

class InstructionTile extends StatelessWidget {
  const InstructionTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
